

// TODO add tests that start to shape the API we would want to use
// with something that provides a simple function to change the route and wraps the "action" interface
// its interface should be {routeName: xyz, params: {}, query: {}} and it should create the URLs for us
// and then trigger the provider to update somehow?
// P

//
