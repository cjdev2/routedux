"use strict";
import React from 'react';
import {shallow} from 'enzyme';

import App from './App';

it('renders a Router for articles and main page', () => {
  // given

  // when
  const wrapper = shallow(
    <App />
  );
});
